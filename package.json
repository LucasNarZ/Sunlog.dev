{
  "name": "Sunlog.dev",
  "version": "1.0.0",
  "description": "**Sunlog.dev** is a modern, interactive blog platform built for developers and learners who love to share knowledge through **HTML**, **CSS**, and **JavaScript**.",
  "scripts": {
    "test": "cd backend && npm run test && cd ..",
    "install-all": "cd frontend && npm i && cd ../backend && npm i && cd ..",
    "start:dev": "docker compose -f docker-compose.dev.yml up -d --build",
    "test:build": "docker compose -f docker-compose.prod.yml build"
  },
  "author": "Lucas",
  "license": "ISC"
}
